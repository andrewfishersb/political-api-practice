import Ember from 'ember';

export default Ember.Component.extend({
  // model() {
  //   return Ember.RSVP.hash({
  //     isHouse: this.store.query('model', { orderBy: 'ca', equalTo: false }),
  //     isSenate: this.store.query('model', { orderBy: 'ca', equalTo: true }),
  //   });
  // },
});
