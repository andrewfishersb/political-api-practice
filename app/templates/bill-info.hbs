{{#each model as |bill|}}
  <h2>{{bill.short_title}}</h2>
  <h3>{{bill.official_title}}</h3>
<p>
  Bill by: <a href="#" {{action 'showSponsor' bill.sponsor_id}}>{{bill.sponsor.first_name}} {{bill.sponsor.last_name}}</a>
</p>
<p>

  introduced on: {{bill.introduced_on}}
</p>


sponsor  {{bill.sponsor_id}}
  <a href={{bill.urls.congress}}>Learn More</a>
{{/each}}
