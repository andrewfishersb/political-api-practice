import { twitter } from 'enlightened-citizentry/helpers/twitter';
import { module, test } from 'qunit';

module('Unit | Helper | twitter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = twitter([42]);
  assert.ok(result);
});
